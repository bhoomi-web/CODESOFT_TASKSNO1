# Source Code

The main implementation is currently in `notebooks/data_cleaning.ipynb`.

A future improvement would be to move reusable cleaning functions into Python modules such as `src/cleaning.py`.
